using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageManager : MonoBehaviour
{
    /// <summary>
    /// 목적지에 도달하면 스테이지 클리어
    /// </summary>
    public bool m_isReachDestination = false;

    /// <summary>
    /// 모든 적을 죽이면 스테이지 클리어
    /// </summary>
    public bool m_isKillallEnemy = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
